#include <stdio.h>
#include <math.h>

void todo(int a, int b, int c) {
    printf("%.0f %.0f %.0f\n", ceil(a), floor(c), round(b));
}

int main()
{
    double ipt[3];
    
    scanf("%lf %lf %lf", &ipt[0], &ipt[1], &ipt[2]);
    
    
    
    return 0;
}
